<h2><%= @article.title %> <%= link_to "🖊️", edit_article_path %> <%= link_to "🗑️",
  article_path(@article),
  method: :delete,
  data: { confirm: "Are you sure?" } %></h2>
<p><%= @article.content%></p>

<%= link_to "Back to List", articles_path %>
